import React from 'react'

const Cartao = (props) => {
    return (
        <div>
            {props.children}
        </div>
    )
}

export default Cartao

const styles = StyleSheet.create({
    
})